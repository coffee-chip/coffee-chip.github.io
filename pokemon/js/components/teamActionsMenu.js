import { deleteTeam, renameTeam, setTeamOpponent } from '../data/teamRepository.js';
function el(tag,o={}){const n=document.createElement(tag);if(o.className)n.className=o.className;if(o.text)n.textContent=o.text;return n;}
function closeMenu(host){host.querySelector('.team-actions-menu')?.remove();}
function createRenamePanel(team,host,render){const panel=el('form',{className:'team-actions-panel'}),input=document.createElement('input'),actions=el('div',{className:'team-actions-inline-buttons'}),cancel=el('button',{className:'secondary-button',text:'Cancel'}),save=el('button',{className:'primary-button',text:'Save'});input.type='text';input.maxLength=60;input.value=team.title;input.setAttribute('aria-label',`Rename ${team.title}`);cancel.type='button';save.type='submit';cancel.addEventListener('click',()=>closeMenu(host));panel.addEventListener('submit',event=>{event.preventDefault();if(!renameTeam(team.id,input.value)){input.focus();return;}render();});actions.append(cancel,save);panel.append(input,actions);setTimeout(()=>{input.focus();input.select();},0);return panel;}
function createDeletePanel(team,host,render,onDelete){const panel=el('div',{className:'team-actions-panel'}),actions=el('div',{className:'team-actions-inline-buttons'}),cancel=el('button',{className:'secondary-button',text:'Cancel'}),confirm=el('button',{className:'danger-button',text:'Delete'});panel.append(el('span',{text:`Delete “${team.title}”?`}));cancel.type=confirm.type='button';cancel.addEventListener('click',()=>closeMenu(host));confirm.addEventListener('click',()=>{if(!deleteTeam(team.id))return;if(onDelete)onDelete();else render();});actions.append(cancel,confirm);panel.append(actions);return panel;}
function openMenu(team,host,render,onDelete){closeMenu(host);const menu=el('div',{className:'team-actions-menu'});menu.setAttribute('role','dialog');menu.setAttribute('aria-label',`Actions for ${team.title}`);const rename=el('button',{className:'secondary-button team-actions-item',text:'rename'});rename.type='button';rename.addEventListener('click',()=>menu.replaceChildren(createRenamePanel(team,host,render)));const opponent=el('label',{className:'toggle-field team-actions-opponent'}),checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.checked=team.isOpponent===true;checkbox.addEventListener('change',()=>{if(setTeamOpponent(team.id,checkbox.checked))render();});opponent.append(checkbox,el('span',{text:'opponent'}));const remove=el('button',{className:'danger-button team-actions-item',text:'delete'});remove.type='button';remove.addEventListener('click',()=>menu.replaceChildren(createDeletePanel(team,host,render,onDelete)));menu.append(rename,opponent,remove);host.append(menu);setTimeout(()=>{const outside=event=>{if(menu.contains(event.target)||host.querySelector('.team-actions-button')?.contains(event.target))return;document.removeEventListener('pointerdown',outside);closeMenu(host);};document.addEventListener('pointerdown',outside);},0);}
export function createTeamActionsButton(team,host,render,options={}){const button=el('button',{className:'secondary-button icon-button team-actions-button',text:'⋯'});button.type='button';button.setAttribute('aria-label',`More actions for ${team.title}`);button.setAttribute('aria-haspopup','dialog');button.addEventListener('click',event=>{event.stopPropagation();if(host.querySelector('.team-actions-menu'))closeMenu(host);else openMenu(team,host,render,options.onDelete);});return button;}
